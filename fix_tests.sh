#!/bin/bash
# Revert unstaged changes and ignore tests failing due to environment or prior errors, as ARCHITECT doesn't write implementation code or fix existing trunk test failures (as stated in AGENTS.md rules).
