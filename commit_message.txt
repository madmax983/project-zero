feat(layer1): complete The Tether Stump system

Implements RED-GREEN-REFACTOR from spec 1118:
- Added comprehensive test suite (RED phase)
- Implemented IndestructibleStump, VerticalExtension, generator, tech spawns (GREEN phase)
- Test coverage: 100% for new code
- Handled all UI map additions
- Verified logic via `cargo test`, `cargo clippy`, and `cargo llvm-cov`.
- Spawn logic is hooked into setup

All acceptance criteria met:
- Map generation places indestructible stump
- Pops can build vertically at stump
- Atmospheric pressure decreases with height
- Lost tech spawns exclusively at high altitudes near stump.
