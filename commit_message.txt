feat(integration): connect trade embargoes to trade routes

INT-1293: Bridges the GalacticMarket embargo state with the Trade Routes system.

Glue added:
- execute_trade_routes_system now checks if the route's item type is embargoed on the GalacticMarket before executing.
- Unaligned faction is used as the default check since routes aren't currently faction-specific.

Integration tests: 1 passing
All unit tests still passing.

Co-Authored-By: google-labs-jules[bot] <161369871+google-labs-jules[bot]@users.noreply.github.com>
