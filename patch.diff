<<<<<<< SEARCH
        // Verify notifications
        let notifications = world.resource::<NotificationQueue>();
        assert_eq!(notifications.messages.len(), 1, "Should generate one notification");
        assert_eq!(notifications.messages[0].severity, crate::layer1::notifications::NotificationSeverity::Error);
=======
        // Verify notifications
        let notifications = world.resource::<NotificationQueue>();
        assert_eq!(notifications.active.len(), 1, "Should generate one notification");
        assert_eq!(notifications.active[0].severity, crate::layer1::notifications::NotificationSeverity::Error);
>>>>>>> REPLACE
