<<<<<<< SEARCH
        // Double check wallet states
        if let Ok(mut w1) = wallets.get_mut(rich) {
            if w1.credits >= amount {
                w1.credits -= amount;

                if let Ok(mut w2) = wallets.get_mut(poor) {
                    w2.credits += amount;

                    let text = format!("Entity {:?} generously transferred {:.1} credits to their good friend Entity {:?} to help them through hard times.", rich, amount, poor);
                    chronicle_events.send(AddChronicleEvent {
                        text,
                        importance: EventImportance::Minor,
                    });
                } else {
                    // Revert if poor wallet disappeared
                    w1.credits += amount;
                }
            }
        }
=======
        // Bevy component mutations need separate mutable get calls if getting two different entities from a query
        let can_afford = if let Ok(w1) = wallets.get(rich) {
            w1.credits >= amount
        } else {
            false
        };

        if can_afford {
            // Check if poor still exists before deducting
            let mut success = false;
            if let Ok(mut w2) = wallets.get_mut(poor) {
                w2.credits += amount;
                success = true;
            }

            if success {
                if let Ok(mut w1) = wallets.get_mut(rich) {
                    w1.credits -= amount;

                    let text = format!("Entity {:?} generously transferred {:.1} credits to their good friend Entity {:?} to help them through hard times.", rich, amount, poor);
                    chronicle_events.send(AddChronicleEvent {
                        text,
                        importance: EventImportance::Minor,
                    });
                }
            }
        }
>>>>>>> REPLACE
