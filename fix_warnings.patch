--- src/layer1/social/placebo.rs
+++ src/layer1/social/placebo.rs
@@ -5,7 +5,7 @@
 use crate::layer1::pop::Pop;
 use crate::layer1::stress::StressTracker;
 use crate::layer1::traits::{Trait, Traits};
-use crate::layer1::notifications::{NotificationQueue, NotificationSeverity};
+use crate::layer1::notifications::NotificationQueue;

 /// Type of Placebo Protocol.
 #[derive(Debug, Clone, Copy, PartialEq, Eq)]
@@ -59,7 +59,7 @@
             }

             // Optional Notification
-            if let (Some(mut queue), Some(sim_time)) = (notification_queue.as_deref_mut(), time.as_deref()) {
+            if let (Some(queue), Some(sim_time)) = (notification_queue.as_deref_mut(), time.as_deref()) {
                 let text = match placebo.protocol {
                     PlaceboProtocol::FakeReinforcements => "The Reinforcements were a lie! Panic Spikes!",
                     PlaceboProtocol::VitaminX => "Vitamin X was sugar! Health Anxiety Spikes!",
