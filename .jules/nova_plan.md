## Fix issues
1. `AtmosphereGrid.get` returns `f32`, not `Option<f32>`?
Let's check `AtmosphereGrid`.
2. Fix `unused import` in `src/layer1/environment/mycorrhizal_network.rs:89`.
3. Fix collapsible if in `src/experimental/toxic_personalities.rs`.
