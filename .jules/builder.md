# Learnt patterns
- When creating tests for systems using `cargo llvm-cov`, ensuring all components used in queries are adequately initialized and populated helps hit the test coverage target.
- Symbiotic structures successfully deduct food using `process_symbiotic_consumption_system`.
