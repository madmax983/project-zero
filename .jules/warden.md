**2024-11-20 - [Logic Panic Hardening & Path Traversal]**
**Threat:** Application DoS via `unwrap()` panics in critical ECS evaluation loops (`blob`, `kinetic_storage`, `permit`, `gene_bank`, `void_signals`, `oral_tradition`, `gpu/evaluate.rs`) which crash the game if expected entities are despawned or components miss. Also found arbitrary File I/O via path traversal in `NarrativeGenerator`.
**Defense:** Replaced unwrap() macros with `let Some(x) = y else { return; }` and `.unwrap_or_default()` strategies to quietly skip entities without exploding server loops. Validated strings in `load_from_files` by ensuring paths are relative and forbid `".."`. Attempted to upgrade `lru` but reverted due to cascading API breaks in dependency tree (`ratatui`/`crossterm`).
