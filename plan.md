1. We have already claimed the task `1035` Modular Fauna.
2. We implemented `CombatStats`, `Armor`, `DamageType` in `src/layer1/combat/stats.rs` and moved the old struct in `rearguard` out of the way. Then we exported it from `src/layer1/mod.rs` so tests compile.
3. We implemented `evaluate_modular_fauna_stats_system` and related types in `src/layer1/fauna/modular_fauna.rs`
4. We verified everything passes tests, clippy, and coverage.
5. Complete pre-commit steps to ensure proper testing, verification, review, and reflection are done.
6. Commit and finalize the task!
