1. **Claim the Task**: Claim task 989 in `design/IN_PROGRESS.md` and commit the change using sed and bash. Verify with `git diff`.
2. **RED Phase**: Create `src/layer1/physics/heat_death.rs` using `cat << 'EOF' > src/layer1/physics/heat_death.rs`, write out the exact tests exactly as defined in the spec `specs/989-heat-death.md` with adjusted imports (omitting `use shared::` parts) and add `pub mod heat_death; pub use heat_death::*;` to `src/layer1/physics/mod.rs` using `sed`. Verify using `cat` and run `cargo test --lib layer1::physics::heat_death` to ensure they fail. Commit the failing tests.
3. **GREEN Phase**: Write the concrete Rust components and systems using `cat << 'EOF' > src/layer1/physics/heat_death.rs` and verify the file contents. Then run `cargo test --lib layer1::physics::heat_death` to confirm tests pass. Ensure no clippy warnings with `cargo clippy -- -D warnings`. Ensure coverage is >= 85% using `cargo llvm-cov --lib -- layer1::physics::heat_death`. Commit this implementation.
4. **Complete pre-commit steps to ensure proper testing, verification, review, and reflection are done**: Run required tools (fmt, check, clippy, test).
5. **Finalize**: Update `design/IN_PROGRESS.md` to move task 989 to `design/COMPLETED.md`. Verify with `git diff`. Finally, run git commit. Use the `submit` tool to push changes.
