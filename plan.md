1. **Move task to IN_PROGRESS.md and claim it**: Use `sed` to remove `266` from `design/BACKLOG.md`. Use `echo` to append the task to `design/IN_PROGRESS.md` with a claim date. Use `cat` to verify changes. Commit and push the changes.
2. **RED Phase**: Use `run_in_bash_session` to create `src/layer1/social/emotional_contagion.rs` and add the test cases described in the RED phase of the spec. Append `pub mod emotional_contagion;` to `src/layer1/social/mod.rs`. Run `cargo test --lib layer1::social::emotional_contagion`. The tests should fail.
3. **GREEN Phase**: Use `run_in_bash_session` to append the `#[derive(Component)] pub struct EmotionalContagion`, `ContagionType` enum, and the `contagion_system` function to `src/layer1/social/emotional_contagion.rs`. Modify `ModModifier::source` to `MoodModifier::label` as it is called in `Morale`. Use `sed` to insert `schedule.add_systems(emotional_contagion::contagion_system);` into the existing schedule block in `src/layer1/social/mod.rs`. Use `cat` to verify the code was appended correctly and `src/layer1/social/mod.rs` was updated properly.
4. **Coverage & Validation**: Check test coverage via `cargo llvm-cov --lib` to ensure the new code reaches >= 85%. Run `cargo fmt`, `cargo check`, `cargo test`, and `cargo clippy -- -D warnings`.
5. **Pre-commit**: Complete pre-commit steps to ensure proper testing, verification, review, and reflection are done.
6. **Complete & Submit**: Use `sed` to remove the task from `design/IN_PROGRESS.md`. Use `echo` to append the task to `design/COMPLETED.md`. Use `cat` to verify changes. Commit the implementation and changes using `git add`, `git commit` and `git push`.
