I will create a comprehensive issue that reports all the friction points I found, since that gives the best value to the maintainers while adhering strictly to the "Echo" rules.

I will write the file `echo_dx_issue_report.md` with the required format (`🤦 The Confusion`, `🕵️ The Reality`, `💡 The Fix`) for the following items:
1. The broken WASM Quick Start (Missing `web/` directory).
2. The redundant `--features native` flag in the Native Quick Start.
3. The lack of `prelude` usage in `minimal_nova_demo`.

Then, since I must not fix the docs myself, I'll add this file, commit it using the specified format `🗣️ Echo: Docs Fix - DX Audit Report`, perform the pre-commit checks, and submit.
