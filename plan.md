1. **Verification**
   - Run `cargo test --all-targets --all-features` to ensure no regressions were introduced.
   - Run `cargo clippy -- -D warnings` to ensure code style is clean.
2. **Pre-Commit Verification**
   - Complete pre-commit steps to ensure proper testing, verification, review, and reflection are done.
3. **Submit**
   - Submit the branch using the submit tool to finalize the task.
