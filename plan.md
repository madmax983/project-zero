1. **Pre-commit Steps**
   - Run `pre_commit_instructions` tool to get and execute necessary verifications.
   - Specifically, ensure `cargo test` and `cargo clippy -- -D warnings` pass.

2. **Submit**
   - Use the `submit` tool to finalize the current set of integrations. Since we've already done `INT-319`, `INT-1034`, `INT-1130`, `INT-1035`, `INT-1290`, `INT-1288`, `INT-761`, `INT-554`, `INT-553`, `INT-1001`, `INT-311`, `INT-354`, `INT-747`, `INT-764`, `INT-765`, `INT-1122`, `INT-355`, and `INT-1086`, we'll submit all these accumulated commits.
