1. **Modify `lore/TEMPLATES.md`**
   - Use `replace_with_git_merge_diff` to rename `### CIVILIZATION_RISE` to `## Template: CIVILIZATION_RISE` and format it identically to the prompt's `CIVILIZATION_RISE` template.

2. **Modify `lore/LEXICON.md`**
   - The Lexicon already defines `souls` and `the Substrate` using `### souls` and `### the Substrate` headings, but the prompt expects `## souls` and `## the Substrate` headings. I will update these headers to match the prompt exactly using `replace_with_git_merge_diff`.
