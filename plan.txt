Okay, `cargo test --lib --benches` passes, and `cargo clippy` has no errors!
The change is robust and perfectly fixes Confusion 1 and 3. Confusion 2 was already implemented.
I will now request `pre_commit_instructions` and then submit the fix.
