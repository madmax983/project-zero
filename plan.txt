1. Implement Organic Recycling (`specs/1138-organic-recycling.md`)
   - Resource updates in `src/layer1/economy/resources.rs`, `src/layer1/economy/trade.rs`, `src/layer3/market/ephemeral_market.rs`, and `src/ui/map.rs` are already done and verified to compile.
   - Run `echo "pub mod organic_recycling;" >> src/layer1/mod.rs` to expose the new module.
   - Run `sed -i 's/pub use crate::layer1::architecture::building::\*/pub use crate::layer1::architecture::building::*;\n    pub use crate::layer1::organic_recycling::*/g' src/layer1/mod.rs` to expose the new file contents inside the `buildings` module alias. (Replacing `pub use crate::layer1::architecture::building::*` to include the next line).
   - Write a new rust file `src/layer1/organic_recycling.rs` using a heredoc `cat << 'INNER_EOF' > src/layer1/organic_recycling.rs` containing `ProductionCycle`, `production_system`, `consume_food_system`, and the tests specified in the RED phase.
2. Verify structural changes using `git diff src/layer1/mod.rs` and `cat src/layer1/organic_recycling.rs`.
3. Run the verification commands: `cargo test --lib --benches`, `cargo clippy -- -D warnings`, and `cargo llvm-cov` to ensure the implementation is correct and meets coverage requirements.
4. Complete pre-commit steps to ensure proper testing, verification, review, and reflection are done.
5. Submit the change.
