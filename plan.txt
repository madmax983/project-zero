1. Register `generate_doomsday_warning` in `src/layer1/systems/execution.rs` (Layer1SystemSet::Execution).
2. Register `handle_ignored_warning` in `src/layer1/systems/execution.rs` (Layer1SystemSet::Execution).
3. Register `validate_prophecy` in `src/layer1/systems/execution.rs` (Layer1SystemSet::Execution).
4. Run `cargo test` and `cargo clippy` to ensure the compilation and tests are passing.
5. Create integration tests `tests/integration/cassandra_syndrome.rs`. Wait, tests are already in `src/layer1/cassandra_syndrome/tests.rs` and bridge tests in `tests/integration/cassandra_syndrome_bridge.rs`.
6. Actually, let's create a test that verifies that they are actually registered correctly in the app schedule, or just run E2E checks if necessary.
7. Update `design/SEAM_MAP.md` and `design/IN_PROGRESS.md`/`design/COMPLETED.md`.
