1. Read the provided specification and existing justice system code.
2. Note that `src/layer1/justice.rs` already contains a `Wanted` system based on `MentalState::Broken(MentalBreakType::Vandalize)` and a `warden_execution_system`. The new spec is introducing `CrimeRecord`, `CrimeCommittedEvent`, `PardonIssuedEvent`, and updating tests.
3. However, the existing `Wanted` struct conflicts with `CrimeRecord`. I will update `Wanted` to `CrimeRecord` and replace the existing tests and logic to match the RED phase spec and then GREEN phase spec exactly as requested by the BUILDER agent instructions. Wait, the spec says to implement the RED-GREEN-REFACTOR for the `justice` module. Let me check the exact state of `src/layer1/justice.rs`. The builder instructions says to "Copy all test cases into the appropriate test module", "Write the SIMPLEST code to make tests pass".
