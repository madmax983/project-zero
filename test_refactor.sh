cat << 'INNER_EOF' >> src/layer1/social/xenoflora_pet.rs

use crate::layer1::resources::ColonyResources;
use crate::layer1::pop::Pop; // Assuming Pop is in crate::layer1::pop or entities
use crate::layer1::GridPosition;
INNER_EOF
