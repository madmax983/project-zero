coverage:
  status:
    project:
      default:
        target: 75%
        threshold: 5%
        informational: false
    patch:
      default:
        target: 70%
        threshold: 10%
        informational: false

comment:
  layout: "header, diff, files"
  behavior: default
  require_changes: false
