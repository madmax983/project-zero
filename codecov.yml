coverage:
  status:
    project:
      default:
        target: 50%
        threshold: 100%
        informational: true
    patch:
      default:
        target: 50%
        threshold: 100%
        informational: true

comment:
  layout: "header, diff, files"
  behavior: default
  require_changes: false
