# 654: The Necro-Industrial Complex

## Overview

The horrifying realization that the most efficient way to fuel your grand stellar expansion is by industrializing the remains of your own dead. Dead Pops can be processed in a "Biomass Sublimator" instead of receiving traditional burials. This yields high-quality organic slurry and trace rare elements essential for advanced Layer 2 ship construction or high-tier agriculture. However, doing so immediately and severely damages the mood of any living Pop who possesses a memory of the deceased. A brutal pirate raid in the system results in massive Layer 1 colony casualties. The resulting massive influx of corpses allows you to instantly fast-track the construction of a massive retaliation dreadnought. However, the surviving colonists are so deeply traumatized by the systematic, industrialized harvesting of their loved ones that they launch a full planetary rebellion just as the new fleet leaves orbit.

## Dependencies

- `537` Funeral Rites

## RED Phase: Tests First

```rust
// src/layer1/economy/necro_industrial_complex.rs

#[cfg(test)]
mod tests {
    use super::*;
    use bevy::pre::App;

    #[test]
    fn test_process_dead_pop() {
        // Arrange
        let mut app = App::new();
        // Setup a dead Pop and a Biomass Sublimator

        // Act
        // Process the dead Pop

        // Assert
        // Expect high-quality organic slurry and trace elements to be yielded
    }

    #[test]
    fn test_process_dead_pop_damages_mood() {
        // Arrange

        // Act
        // Process a dead Pop

        // Assert
        // Expect the mood of living Pops with memories of the deceased to be severely damaged
    }

    #[test]
    fn test_mass_harvesting_causes_rebellion() {
        // Arrange

        // Act
        // Process a massive amount of dead Pops

        // Assert
        // Expect a massive drop in colony morale, potentially leading to a rebellion
    }
}
```

## GREEN Phase: Minimal Implementation

```rust
// src/layer1/economy/necro_industrial_complex.rs

// Minimal event and system structures to make the tests pass.
// e.g., define BiomassSublimator component, ProcessDeadPopEvent, etc.
```

## REFACTOR Phase: Quality & Design

- Balance the yield of processing dead Pops against the severe mood penalty and rebellion risk.
- Integrate the processed resources into advanced Layer 2 construction and agriculture.
- Consider how the presence of Biomass Sublimators affects the colony's social dynamics and reputation.

## Acceptance Criteria (Testable!)

- [ ] All tests in RED phase pass
- [ ] `cargo test` returns 0 failures
- [ ] `cargo clippy -- -D warnings` passes
- [ ] Test coverage ≥85% for new code
- [ ] Dead Pops can be processed in Biomass Sublimators for resources.
- [ ] Processing dead Pops damages the mood of living Pops with memories of the deceased.
- [ ] Mass processing of dead Pops can cause significant drops in colony morale and lead to rebellion.

## Technical Guidance

- Use Bevy's ECS to handle the conversion of dead Pops into resources through the Biomass Sublimator.
- The Utility AI and Morale systems will need to be updated to react strongly to the processing of loved ones.
- The Layer 2 construction system should recognize and require the specific resources generated by the sublimators.

## Questions

*Builder: add questions here if spec is unclear.*
