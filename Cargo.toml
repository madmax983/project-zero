[package]
name = "project-zero"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = "0.17.3"

# Enable dynamic linking for faster compile times during development
[features]
default = []
dev = ["bevy/dynamic_linking"]
