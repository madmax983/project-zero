1. **Identify the missing seam**: The `289-mimetic-plague` spec states that Memetic Infections spread via `ConversationEvent` triggered by social interactions. The `process_memetic_transmission_system` exists in `src/layer1/memetics/memetic_hazards.rs` and listens for `ConversationEvent`s, but no system emits this event.
2. **Review `proximity_social_system`**: The `proximity_social_system` in `src/layer1/social/mod.rs` evaluates social relationships when Pops are close (distance <= 2). It's the perfect place to emit a `ConversationEvent` when two Pops interact (e.g., when they are close).
3. **Modify `proximity_social_system`**: Add `EventWriter<crate::layer1::memetics::memetic_hazards::ConversationEvent>` as a parameter to `proximity_social_system`. Inside the loop, when two Pops are close (distance <= 2), emit a `ConversationEvent` with `initiator: entity` and `receiver: other_entity`. Note: Since it's O(N^2), we might want to only emit events for one direction (e.g., `entity < other_entity`) to avoid duplicate conversations, or just let it emit for both. The spec test uses `SocialInteractionEvent` but the code uses `ConversationEvent`. Let's emit `ConversationEvent`.
4. **Integration Test**: Create a new integration test in `tests/integration/memetic_plague_transmission.rs` to verify the seam: Pops in close proximity trigger `proximity_social_system`, which emits `ConversationEvent`, which is then processed by `process_memetic_transmission_system` to spread the `MemeticInfection`. Register the test module in `tests/integration.rs`.
5. **Update `SEAM_MAP.md`**: Document `INT-289: The Mimetic Plague -> Social Interaction`.
6. **Pre-commit**: Complete pre-commit steps to ensure proper testing, verification, review, and reflection are done.
