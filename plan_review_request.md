The plan is to implement the "Empty" Room specification (1297).
1. Create `src/layer1/architecture/empty_room.rs`.
2. Implement `SanctuaryZone` and `evaluate_sanctuary_emptiness`, `apply_sanctuary_stress_relief`. Note that we are using `ZoneGrid` for zone detection and `ClutterGrid` for clutter check, we should not add mock components in the test if actual components like `ZoneGrid` can be used. But the spec provided RED tests, so I will copy them over verbatim and make adjustments only in the GREEN/REFACTOR phases or in the main logic to integrate with real global components.
3. Oh wait, the test components like `SanctuaryZone`, `Clutter`, `Stress`, `InZone` are mocked in the spec tests. I'll include the mock tests in RED phase, then Refactor to use the global systems. Wait, the builder rule says: "Specifications may use simplified or mock ECS components in their RED phase test examples for brevity. During the GREEN and REFACTOR phases, you must replace these local mock definitions with the actual global game components (e.g., crate::layer1::pop::Pop, crate::layer1::utility_types::AssignmentType) to ensure proper integration with the Bevy engine and to pass code review."
4. I will create `src/layer1/architecture/empty_room.rs` with:
   - RED phase: I'll include the exact tests from the spec in `tests` module, but I'll need to adapt them to compile. The spec has mocked components. I will create private or `pub(crate)` mock structs in the `tests` module so they compile in the RED phase.
   - GREEN phase: implement the system. Wait, since I have to use actual global components in GREEN/REFACTOR, should I update the tests to use actual global components? The memory says "During the GREEN and REFACTOR phases, you must replace these local mock definitions with the actual global game components". This means in the GREEN phase implementation I'll use real components, and the tests should be updated to test against the real implementation. The RED phase tests might need to be rewritten to use global components so they pass with the GREEN implementation.
   - Real components to use:
     - Zones: `ZoneGrid` from `crate::layer1::zone::ZoneGrid` instead of `SanctuaryZone` component.
     - Clutter: `ClutterGrid` from `crate::layer1::clutter::ClutterGrid` instead of `Clutter` component. Or we could use entities that have `Building` or `Item`. Wait, the spec says "Sanctuary zones correctly toggle their active state based on presence of items/furniture." We can use `BuildingMap` or `OccupiedTiles` for furniture, and `ClutterGrid` or `Query<&Item, &GridPosition>` for items. Actually, let's use `ZoneGrid` to identify Sanctuary zones, and we can maintain an `ActiveSanctuaries` resource or just calculate it per-tick.
     - Stress: `crate::layer1::stress::StressTracker` instead of `Stress`.
     - InZone: `crate::layer1::map::GridPosition` to check if a pop is in the Sanctuary zone.
5. So `src/layer1/architecture/empty_room.rs` will contain the new systems `evaluate_sanctuary_emptiness` and `apply_sanctuary_stress_relief`. Since `ZoneGrid` is cell-based, we can iterate over all Pops. If a Pop is on a tile with `ZoneType::Sanctuary`, we check if that sanctuary is empty. A sanctuary could be a contiguous region of `ZoneType::Sanctuary`. But the spec says `mut q_zones: Query<(Entity, &mut SanctuaryZone)>`. The spec assumes zones are entities. But in `ZoneGrid`, zones are just tile properties. Let's look at the refactor phase: "Optimize evaluate_sanctuary_emptiness... integrating seamlessly with Layer 1's entity categorization." I will use `ZoneGrid` and `GridPosition`.
6. Pre-commit check.
